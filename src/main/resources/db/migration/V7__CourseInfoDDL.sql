CREATE TABLE course_base_info
  (
     course_id   VARCHAR(15),
     course_name VARCHAR(30),
     department  VARCHAR(20),
     professor   VARCHAR(50),
     coordinator VARCHAR(50),
     coord_email VARCHAR(40),
     PRIMARY KEY (course_id)
  );